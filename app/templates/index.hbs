<h1>Listings</h1>
<div class="col-sm-2">
  {{new-listing-category categories=model.categories}}
{{log model.categories.length}}
</div>
<div class="col-sm-10">
  <ul>
    {{#each model.listings as |listing|}}
    <p>{{listing-tile listing=listing}}</p>
    {{/each}}
  </ul>
</div>
